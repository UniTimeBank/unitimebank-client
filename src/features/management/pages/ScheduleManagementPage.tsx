import React from 'react';
import { MentorScheduleManager } from '@/features/schedule/components';

export const ScheduleManagementPage: React.FC = () => {
  return (
    <div className="animate-in fade-in duration-200">
      <MentorScheduleManager />
    </div>
  );
};
